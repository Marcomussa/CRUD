<%- include('./partials/head.ejs') %> 

<%- include('./partials/nav.ejs') %> 

<body>
    <div class="container">
        <div class="row containerProdDetail background">
            <div class="col-md-4">
                <img src="../img/avatars/<%= products.img %> " alt="producto">
            </div>
            <div class="col-md-8">
                <h3><%= products.name %> </h3>
                <hr>
                <h4><%= products.description %> </h4>
                <hr>
                <h4>Precio: $<%= products.price %> </h4>
                <h4>Descuento: % <%= products.discount %> </h4>
                <h4>Final: $<%= products.priceWithDiscount %> </h4>
                <div class="mt-3">
                    <form action="">
                        <input type="submit" value="Agregar al Carrito" class="btn btn-success"> 
                    </form>
                    <form action="">
                        <input type="submit" class="btn btn-primary" value="Wishlist">
                    </form>
                </div>
            </div>
            <div class="col-md-4"></div>
        </div>
    </div>
</body>

<%- include('./partials/footer.ejs') %> 
